import { Shop2GwaliorValidators } from './shop2-gwalior-validators';

describe('Shop2GwaliorValidators', () => {
  it('should create an instance', () => {
    expect(new Shop2GwaliorValidators()).toBeTruthy();
  });
});
